//
// AUTO-GENERATED FILE, DO NOT MODIFY!
//

// ignore_for_file: unused_element
import 'package:json_annotation/json_annotation.dart';

/// All possibility of string to use
enum OwnCapability {
  /// All possibility of string to use
  @JsonValue(r'block-users')
  blockUsers(r'block-users'),

  /// All possibility of string to use
  @JsonValue(r'change-max-duration')
  changeMaxDuration(r'change-max-duration'),

  /// All possibility of string to use
  @JsonValue(r'create-call')
  createCall(r'create-call'),

  /// All possibility of string to use
  @JsonValue(r'create-reaction')
  createReaction(r'create-reaction'),

  /// All possibility of string to use
  @JsonValue(r'enable-noise-cancellation')
  enableNoiseCancellation(r'enable-noise-cancellation'),

  /// All possibility of string to use
  @JsonValue(r'end-call')
  endCall(r'end-call'),

  /// All possibility of string to use
  @JsonValue(r'join-backstage')
  joinBackstage(r'join-backstage'),

  /// All possibility of string to use
  @JsonValue(r'join-call')
  joinCall(r'join-call'),

  /// All possibility of string to use
  @JsonValue(r'join-ended-call')
  joinEndedCall(r'join-ended-call'),

  /// All possibility of string to use
  @JsonValue(r'kick-user')
  kickUser(r'kick-user'),

  /// All possibility of string to use
  @JsonValue(r'mute-users')
  muteUsers(r'mute-users'),

  /// All possibility of string to use
  @JsonValue(r'pin-for-everyone')
  pinForEveryone(r'pin-for-everyone'),

  /// All possibility of string to use
  @JsonValue(r'read-call')
  readCall(r'read-call'),

  /// All possibility of string to use
  @JsonValue(r'remove-call-member')
  removeCallMember(r'remove-call-member'),

  /// All possibility of string to use
  @JsonValue(r'screenshare')
  screenshare(r'screenshare'),

  /// All possibility of string to use
  @JsonValue(r'send-audio')
  sendAudio(r'send-audio'),

  /// All possibility of string to use
  @JsonValue(r'send-closed-captions-call')
  sendClosedCaptionsCall(r'send-closed-captions-call'),

  /// All possibility of string to use
  @JsonValue(r'send-video')
  sendVideo(r'send-video'),

  /// All possibility of string to use
  @JsonValue(r'start-broadcast-call')
  startBroadcastCall(r'start-broadcast-call'),

  /// All possibility of string to use
  @JsonValue(r'start-closed-captions-call')
  startClosedCaptionsCall(r'start-closed-captions-call'),

  /// All possibility of string to use
  @JsonValue(r'start-frame-record-call')
  startFrameRecordCall(r'start-frame-record-call'),

  /// All possibility of string to use
  @JsonValue(r'start-record-call')
  startRecordCall(r'start-record-call'),

  /// All possibility of string to use
  @JsonValue(r'start-transcription-call')
  startTranscriptionCall(r'start-transcription-call'),

  /// All possibility of string to use
  @JsonValue(r'stop-broadcast-call')
  stopBroadcastCall(r'stop-broadcast-call'),

  /// All possibility of string to use
  @JsonValue(r'stop-closed-captions-call')
  stopClosedCaptionsCall(r'stop-closed-captions-call'),

  /// All possibility of string to use
  @JsonValue(r'stop-frame-record-call')
  stopFrameRecordCall(r'stop-frame-record-call'),

  /// All possibility of string to use
  @JsonValue(r'stop-record-call')
  stopRecordCall(r'stop-record-call'),

  /// All possibility of string to use
  @JsonValue(r'stop-transcription-call')
  stopTranscriptionCall(r'stop-transcription-call'),

  /// All possibility of string to use
  @JsonValue(r'update-call')
  updateCall(r'update-call'),

  /// All possibility of string to use
  @JsonValue(r'update-call-member')
  updateCallMember(r'update-call-member'),

  /// All possibility of string to use
  @JsonValue(r'update-call-permissions')
  updateCallPermissions(r'update-call-permissions'),

  /// All possibility of string to use
  @JsonValue(r'update-call-settings')
  updateCallSettings(r'update-call-settings'),

  /// All possibility of string to use
  @JsonValue(r'unknown_default_open_api')
  unknownDefaultOpenApi(r'unknown_default_open_api');

  const OwnCapability(this.value);

  final String value;

  @override
  String toString() => value;
}
