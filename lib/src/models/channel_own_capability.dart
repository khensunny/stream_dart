//
// AUTO-GENERATED FILE, DO NOT MODIFY!
//

// ignore_for_file: unused_element
import 'package:json_annotation/json_annotation.dart';

/// All possibility of string to use
enum ChannelOwnCapability {
  /// All possibility of string to use
  @JsonValue(r'ban-channel-members')
  banChannelMembers(r'ban-channel-members'),

  /// All possibility of string to use
  @JsonValue(r'cast-poll-vote')
  castPollVote(r'cast-poll-vote'),

  /// All possibility of string to use
  @JsonValue(r'connect-events')
  connectEvents(r'connect-events'),

  /// All possibility of string to use
  @JsonValue(r'create-attachment')
  createAttachment(r'create-attachment'),

  /// All possibility of string to use
  @JsonValue(r'delete-any-message')
  deleteAnyMessage(r'delete-any-message'),

  /// All possibility of string to use
  @JsonValue(r'delete-channel')
  deleteChannel(r'delete-channel'),

  /// All possibility of string to use
  @JsonValue(r'delete-own-message')
  deleteOwnMessage(r'delete-own-message'),

  /// All possibility of string to use
  @JsonValue(r'delivery-events')
  deliveryEvents(r'delivery-events'),

  /// All possibility of string to use
  @JsonValue(r'flag-message')
  flagMessage(r'flag-message'),

  /// All possibility of string to use
  @JsonValue(r'freeze-channel')
  freezeChannel(r'freeze-channel'),

  /// All possibility of string to use
  @JsonValue(r'join-channel')
  joinChannel(r'join-channel'),

  /// All possibility of string to use
  @JsonValue(r'leave-channel')
  leaveChannel(r'leave-channel'),

  /// All possibility of string to use
  @JsonValue(r'mute-channel')
  muteChannel(r'mute-channel'),

  /// All possibility of string to use
  @JsonValue(r'pin-message')
  pinMessage(r'pin-message'),

  /// All possibility of string to use
  @JsonValue(r'query-poll-votes')
  queryPollVotes(r'query-poll-votes'),

  /// All possibility of string to use
  @JsonValue(r'quote-message')
  quoteMessage(r'quote-message'),

  /// All possibility of string to use
  @JsonValue(r'read-events')
  readEvents(r'read-events'),

  /// All possibility of string to use
  @JsonValue(r'search-messages')
  searchMessages(r'search-messages'),

  /// All possibility of string to use
  @JsonValue(r'send-custom-events')
  sendCustomEvents(r'send-custom-events'),

  /// All possibility of string to use
  @JsonValue(r'send-links')
  sendLinks(r'send-links'),

  /// All possibility of string to use
  @JsonValue(r'send-message')
  sendMessage(r'send-message'),

  /// All possibility of string to use
  @JsonValue(r'send-poll')
  sendPoll(r'send-poll'),

  /// All possibility of string to use
  @JsonValue(r'send-reaction')
  sendReaction(r'send-reaction'),

  /// All possibility of string to use
  @JsonValue(r'send-reply')
  sendReply(r'send-reply'),

  /// All possibility of string to use
  @JsonValue(r'send-restricted-visibility-message')
  sendRestrictedVisibilityMessage(r'send-restricted-visibility-message'),

  /// All possibility of string to use
  @JsonValue(r'send-typing-events')
  sendTypingEvents(r'send-typing-events'),

  /// All possibility of string to use
  @JsonValue(r'set-channel-cooldown')
  setChannelCooldown(r'set-channel-cooldown'),

  /// All possibility of string to use
  @JsonValue(r'share-location')
  shareLocation(r'share-location'),

  /// All possibility of string to use
  @JsonValue(r'skip-slow-mode')
  skipSlowMode(r'skip-slow-mode'),

  /// All possibility of string to use
  @JsonValue(r'slow-mode')
  slowMode(r'slow-mode'),

  /// All possibility of string to use
  @JsonValue(r'typing-events')
  typingEvents(r'typing-events'),

  /// All possibility of string to use
  @JsonValue(r'update-any-message')
  updateAnyMessage(r'update-any-message'),

  /// All possibility of string to use
  @JsonValue(r'update-channel')
  updateChannel(r'update-channel'),

  /// All possibility of string to use
  @JsonValue(r'update-channel-members')
  updateChannelMembers(r'update-channel-members'),

  /// All possibility of string to use
  @JsonValue(r'update-own-message')
  updateOwnMessage(r'update-own-message'),

  /// All possibility of string to use
  @JsonValue(r'update-thread')
  updateThread(r'update-thread'),

  /// All possibility of string to use
  @JsonValue(r'upload-file')
  uploadFile(r'upload-file'),

  /// All possibility of string to use
  @JsonValue(r'unknown_default_open_api')
  unknownDefaultOpenApi(r'unknown_default_open_api');

  const ChannelOwnCapability(this.value);

  final String value;

  @override
  String toString() => value;
}
